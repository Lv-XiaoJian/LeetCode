#include "BaoShuGame.h"

int CBaoShuGame::GamePlay(int& nNum)
{
	//int nPeople[100] = {0};
	//for (size_t i = 0; i < 100; i++)
	//	nPeople[i] = i + 1;

	//int* pStart,pEnd = nPeople;
	//pStart = nPeople;
	//while (nPeople > nNum)
	//{



	//}
	return 0;
}
